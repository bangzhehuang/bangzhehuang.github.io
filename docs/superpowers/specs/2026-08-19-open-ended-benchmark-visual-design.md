# Open-Ended Benchmark Homepage Visual

## Purpose

Create a homepage project image that communicates one idea at thumbnail size:
different valid research trajectories can converge on the same stable physical
ground truth.

## Composition

- Use a 16:9 canvas with a white background.
- Begin with one research-agent state on the left.
- Branch into three visibly different polyline trajectories across the center.
- Represent simulation, observation, and feedback as compact geometric nodes,
  without explanatory sentences or tiny labels.
- Converge all three trajectories on one physical-structure target at the right.
- Make the target resemble a low-dimensional lattice or interaction pattern,
  so it reads as physical ground truth rather than a generic bullseye.

## Visual System

- Use black and neutral gray for trajectories and intermediate states.
- Reserve one restrained blue (`#28678F`) for the shared ground truth.
- Use straight segments and folded polylines; do not use decorative curves.
- Keep strokes and nodes strong enough to survive the homepage thumbnail size.
- Avoid gradients, shadows, decorative cards, dense text, and colorful accents.

## Homepage Integration

- Add the visual to the third research entry, replacing its text-only layout.
- Preserve the existing research-card proportions and spacing.
- Update the project metadata to:
  `Independent research · Academic discussion: Prof. Siheng Chen, School of
  Artificial Intelligence, Shanghai Jiao Tong University · Pilot completed`.
- Visually inspect the asset and the project card; no broad regression test run
  is required for this content-only update.
